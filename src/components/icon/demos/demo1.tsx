import { View } from '@tarojs/components';
import React from 'react';

const CheckIcon = () => {
  return <View className="icon iconfont icon-check" />;
};

export default () => {
  // 调用方式举一个例子
  return <CheckIcon />;
};
